Task 1 Election Result

Create a mavean project 
name election-analysis

pom.xml
  add the csv apache dependency

       <!-- Apache Commons CSV -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-csv</artifactId>
            <version>1.10.0</version>
        </dependency>

create packages like 
 |- com.election.task
    |-model
	->CandidateResult.java
	->ElectionResult.java
    |-service
	->ElectionService.java
    |-util
	->ResultDataReader.java
    -> Main.java

 |- resources
    ->2019_Results.csv
    ->2019_Result_Winning_Candidate.csv

 ->add the file in resources folder 
 go to resources 
  -> right click -> import -> General 
    -> File System -> Browse Form Directory and select the csv files
      -> Finish

step 1:-
 Created 2 POJO Class
        ->CandidateResult.java
	->ElectionResult.java
step 2:-
 Read the Data From CSV file
 we have 2 file so i created 2 method 
 and 1 private method which read the csv file and 
 use in other method 
----------------------
private CSVParser getParser(String fileName) throws IOException{
		
		InputStream input = getClass()
				.getClassLoader()
				.getResourceAsStream(fileName);
		
		if (input == null) {
		    throw new IOException("File not found: " + fileName);
		}
		
		
		BufferedReader reader = new BufferedReader(new InputStreamReader(input, StandardCharsets.UTF_8));
		
		CSVParser parser = CSVFormat.DEFAULT
				.builder()
				.setHeader()
				.setSkipHeaderRecord(true)
				.build()
				.parse(reader);
		
		return parser;
	}
------------------------

step 3:- Create the service class where all the task question are there 

   -> task1 ->
method take 2 value List<ElectionResult> winners, List<CandidateResult> candidates

in this method first calculate the total votes 
 
